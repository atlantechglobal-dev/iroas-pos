node_modules/
dist/
.env
.env.*
!.env.example
NUL.css

# SQLite database files (each environment — local dev, the live VM — has
# its own; these must never be tracked or they'll conflict/overwrite real data)
server/data/*.db
server/data/*.db-journal
server/data/*.db-wal
server/data/*.db-shm